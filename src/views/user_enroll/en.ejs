<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Welcome to ZYNQ</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap');

    body {
      margin: 0;
      padding: 0;
      background-color: #f9f5fb;
      font-family: 'Poppins', sans-serif;
    }

    .container {
      max-width: 620px;
      margin: 40px auto;
      background: #fff;
      border-radius: 14px;
      overflow: hidden;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
    }

    .header {
      background: linear-gradient(135deg, #efc8ec, #fbe8f5);
      padding: 40px 20px;
      text-align: center;
    }

    .header img {
      width: 150px;
      height: auto;
    }

    .body {
      padding: 35px 40px;
      text-align: center;
    }

    h1 {
      font-size: 22px;
      color: #333333;
      font-weight: 600;
      margin-bottom: 20px;
    }

    p {
      font-size: 14px;
      color: #555555;
      line-height: 1.7;
      margin: 10px 0;
    }

    .highlight {
      color: #d65a8a;
      font-weight: 600;
    }

    .table {
      margin: 25px auto;
      text-align: left;
      width: 100%;
    }

    .table td {
      padding: 10px 0;
      font-size: 14px;
      border-bottom: 1px solid #eee;
    }

    .table td:first-child {
      font-weight: 500;
      width: 150px;
      color: #444;
    }

    a {
      color: #d65a8a;
      text-decoration: none;
    }

    .footer {
      background: #2b2b2b;
      color: white;
      text-align: center;
      padding: 25px;
    }

    .footer a {
      color: #fff;
      font-size: 13px;
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <img src="<%= image_logo %>" alt="ZYNQ Logo">
    </div>

    <div class="body">
      <h1>Welcome to ZYNQ!</h1>
      <p><strong><%= email %></strong>, we just saw your first interaction and we’re excited to onboard you 🎉</p>
      <p>You're now officially part of the <span class="highlight">ZYNQ</span> family — where next-gen skin diagnostics meets professional aesthetic care.</p>

      <p>Here are your enrollment details:</p>
      <table class="table">
        <tr>
          <td>Email:</td>
          <td><%= email %></td>
        </tr>

        <% if (application_type === "android" || application_type === "both") { %>
        <tr>
          <td>Android App:</td>
          <td><a href="<%= android_app_link %>">Download Android App</a></td>
        </tr>
        <% } %>

        <% if (application_type === "ios" || application_type === "both") { %>
        <tr>
          <td>iOS App:</td>
          <td><a href="<%= ios_app_link %>">Download iOS App</a></td>
        </tr>
        <% } %>
      </table>

      <% if (application_type === "ios" || application_type === "both") { %>
      <p>Your UDID will be added within 24 hours.</p>
      <% } %>

      <p>Once you're in, you'll get your first personalized skin diagnosis and treatment suggestions instantly — powered by AI and expert dermatology.
      </p>

      <p>We're so glad to have you here 💫</p>

      <p style="margin-top: 30px;">Cheers,<br><strong>ZYNQ Team</strong></p>
    </div>

    <div class="footer">
      <p>Need More Help?</p>
      <a href="#">We are here to help you out</a>
    </div>
  </div>
</body>
</html>
